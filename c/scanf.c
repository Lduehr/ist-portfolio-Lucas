#include <stdio.h>
int main()
{
	int age;
	float gpa;
	char grade;
	printf ("enter your age: ");
	scanf ("%i", &age);

	printf("your gpa now: ");
	scanf (" %f", &gpa);

	printf("your leter grade: ");
	scanf (" %c", &grade);

	return 0;
}
